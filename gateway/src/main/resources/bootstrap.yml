server:
  port: 8085
spring:
  application:
    name: gateway
  profiles:
    active: discovery, gateway, gateway-paths
  cloud:
    nacos:
      config:
        server-addr: http://39.97.237.248:8848
        file-extension: yml   #后最
        prefix: ysu         #前缀