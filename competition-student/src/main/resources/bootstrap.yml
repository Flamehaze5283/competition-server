server:
  port: 8102
spring:
  application:
    name: student
  profiles:
    active: discovery, datasource, mybatis, ribbon, redis
  cloud:
    nacos:
      config:
        server-addr: http://39.97.237.248:8848
        file-extension: yml   #后最
        prefix: ysu         #前缀
